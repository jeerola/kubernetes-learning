apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: exercises
  name: postgres-claim
spec:
  storageClassName: postgres-pv
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
