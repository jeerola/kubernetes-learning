apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hash-claim # name of the volume claim, this will be used in the deployment
spec:
  storageClassName: persistent-volume # this is the name of the persistent volume we are claiming
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
